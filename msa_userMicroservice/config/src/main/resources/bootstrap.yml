encrypt:
  # key: abcdefghijklmnopqrstuxyz0123456789   # 대칭키에 사용할 수 있는 key 지정
  # test
  # localhost:8888/encrypt (POST) param: Body (위 대칭키로 암호화된 response 확인 가능)
  # localhost:9999/decrypt (POST) param: Body (위 대칭키로 복호화된 response 확인 가능)
  key-sotre:
    location: file://${user.home}/Documents/seohae-dev/config/keystore/apiEncryptionKey.jks
    password: test1234
    alias: apiEncryptionKey


