package Lecture01._2_

// Primitive Type
fun main() {
    // primitive type, reference type
    // 숫자 문자, 불리언과 같은 몇몇 타입은 내부적으로 특ㅂ멸한 표현을 갖는다.
    // 이 타입들은 실행시에 primitive type으로 보이지만 평범한 클래스다.
    var number1: Long = 10L // 상황에 따라서 Long -> long으로 변환해준다.

    // 프로그래머가 boxing, unboxing을 고려하지 않아도 되도록 kotlin이 알아서 변환해준다.
}