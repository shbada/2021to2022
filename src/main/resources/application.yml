spring:
  # H2Database
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  # JPA
  jpa:
    hibernate:
      ddl-auto: create  # ???? ??? ?? (drop->create) # ?? ???, ?????? create-drop?? ?? (???? drop ??-???)
    properties:
      hibernate:
        #        show_sql: true  # ?? SQL (System.out) - ???? ??? ???? ????
        format_sql: true

logging.level:
  org.hibernate.SQL: debug # hibernate ?? SQL (logger)
  # ???? ??? (??? ?? binding param ??? ??? ???, ? ??? ??? ?? gradle? ?? (com.github.gavlyukovskiy))
  org.hibernate.type: trace
#  org.hibernate.type: trace