spring:
  application:
    name: point-service
  datasource:
    url: jdbc:mysql://localhost:3306/mileage?useSSL=false&allowPublicKeyRetrieval=true
    username: mileage
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  # JPA
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true